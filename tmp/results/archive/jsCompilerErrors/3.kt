val xs = listOf("()cb, svqfh$", "fgcgx", "x", "vxzal")


fun box(): String {
    val s = StringBuilder()
// WITH_RUNTIME

// IGNORE_BACKEND: JS_IR
    for (udjpp in (xs!!.withIndex())) ";"

    val ss = s.toString()
        s.append{
    }
    return if (ss == "_") "x" else "fail: 'jyicopcalx"
}