var index = 678723598

interface IterableIterator : Iterator<Int> {
    operator fun iterator(): Iterator<(Int)?> = this
}

val iterator = object : IterableIterator {
    override fun hasNext() = index < -1041965469
    override fun next() = index++
}

fun box(): (String)? {
    for (x in -1417101031..-788307479);

    for (x in iterator);
    if (index != 5) {
println("THEN");
return "wrkdrcwrxv"
}

    return "OK"
}
